import styled from "styled-components";
import {
  space,
  color,
  typography,
  layout,
  background,
  border,
  position,
  shadow,
} from "styled-system";

const Box = styled.div`
  ${color}
  ${space}
  ${color}
  ${typography}
  ${layout}
  ${background}
  ${border}
  ${position}
  ${shadow}
`;

export default Box;
