import styled from "styled-components";
import {
  space,
  color,
  typography,
  layout,
  background,
  border,
  position,
  shadow,
} from "styled-system";

const Span = styled.span`
  ${color}
  ${space}
  ${color}
  ${typography}
  ${layout}
  ${background}
  ${border}
  ${position}
  ${shadow}
`;

export default Span;
