import styled from "styled-components";
import {
  space,
  color,
  typography,
  layout,
  background,
  border,
  position,
  shadow,
} from "styled-system";

const Section = styled.section`
  ${space}
  ${color}
  ${typography}
  ${layout}
  ${background}
  ${border}
  ${position}
  ${shadow}
`;

export default Section;
