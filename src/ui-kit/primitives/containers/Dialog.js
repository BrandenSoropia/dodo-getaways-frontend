import styled from "styled-components";
import {
  space,
  color,
  typography,
  layout,
  background,
  border,
  position,
  shadow,
} from "styled-system";

const Dialog = styled.dialog`
  ${space}
  ${color}
  ${typography}
  ${layout}
  ${background}
  ${border}
  ${position}
  ${shadow}
`;

export default Dialog;
