import styled from "styled-components";
import { space, color, layout, background, border, position, shadow } from "styled-system";

const Image = styled.img`
  ${"" /* Default styles */}
  height: 100%;
  width: 100%;

  ${space}
  ${color}
  ${layout}
  ${background}
  ${border}
  ${position}
  ${shadow}
`;

export default Image;
